fireaway.csml.co.za {
        handle /api/* {
                reverse_proxy prod-python-backend:8000
        }

        handle /health {
                reverse_proxy prod-python-backend:8000
        }

        handle {
                reverse_proxy prod-next-frontend:3000
        }

        tls /etc/caddy/cloudflare-origin.pem /etc/caddy/cloudflare-origin.key
}